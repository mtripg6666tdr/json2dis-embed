{
  "name": "json2dis-embed",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf -f dist && tsc -p tsconfig.json",
    "postinstall": "npm run build",
    "start": "node dist/lib.js"
  },
  "author": "mtripg6666tdr",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^14.14.25",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  }
}
